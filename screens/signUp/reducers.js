let initialStage = {
    user: null,
    error: false,
    isLoading: false,
    errorMessgae: null,
}

const signUp = (state = initialStage, actions) => {
    switch (actions.type) {
        default:
            return state
    }
}

export default signUp
